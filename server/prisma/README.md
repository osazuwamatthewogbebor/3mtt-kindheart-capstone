# Prisma & Database
This folder contains our database configuration and schema.
- `schema.prisma`: Defines our database models.
- `seed.js`: Used to populate the database with initial data (like categories).
- `migrations/`: (Generated automatically) Contains our database history. **Always commit this folder to Git** so we all stay in sync.
