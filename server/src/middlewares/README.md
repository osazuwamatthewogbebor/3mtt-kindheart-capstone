# Middlewares
This folder contains "Gatekeeper" functions that run before our controllers.
- `isAuth.js`: Checks if a user is logged in.
- `isAdmin.js`: Checks if a user has admin privileges.
- `errorMiddleware.js`: Handles global errors.
